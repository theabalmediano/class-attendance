import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Class Attendance Tracker',
  webDir: 'dist'
};

export default config;
